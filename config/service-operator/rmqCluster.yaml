apiVersion: rabbitmq.com/v1beta1
kind: RabbitmqCluster
metadata:
  annotations:
    rabbitmq.com/topology-allowed-namespaces: '*'
  name: dev-tap
  namespace: dev-tap
spec:
  replicas: 1
  resources:
    requests:
      cpu: 200m
    limits:
      cpu: 200m
